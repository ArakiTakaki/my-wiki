import React from 'react';
const a = require('./Modal.sass');

interface Props {
  children?: any;
}
const Modal = (props: Props) => {
  return <h1>SampleasdfFunctionalComponent</h1>;
};

export default Modal;
